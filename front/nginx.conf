server {
    listen 80;
    server_name _;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name techtrackinvest.ru www.techtrackinvest.ru;

    ssl_certificate /etc/ssl/techtrackinvest.ru.crt;
    ssl_certificate_key /etc/ssl/techtrackinvest.ru.key;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;
    keepalive_timeout 70;

    root /usr/share/nginx/html;
    index index.html;

    # Сжатие
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;


    location /api/ {
        proxy_pass http://techtrackinvest.ru:8080;  
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Origin $http_origin;
        proxy_set_header Authorization $http_authorization;
    }

    # Кэширование статических файлов
    location /static/ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # Обработка React Router
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Запрет доступа к .env файлам
    location ~ /\.env {
        deny all;
        return 404;
    }
    
} 